[project]
name = "auto-google-forms"
version = "1.0.0"
description = "CLI and backend core for authorized Google Forms"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "questionary>=2.1.1",
    "requests>=2.34.2",
]

[project.scripts]
forms-tool = "auto_google_forms.cli:main"
